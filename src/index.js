import React from 'react';
import ReactDOM from 'react-dom';
import { Menu } from './card/index';

ReactDOM.render(
  <React.StrictMode>
    <Menu />
  </React.StrictMode>,
  document.getElementById('root')
);